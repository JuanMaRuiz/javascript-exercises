const assert = require('assert'),
      chai = require('chai'),
      expect = chai.expect;

describe('Write a JavaScript program to convert temperatures to and from celsius, fahrenheit', function() {
    it('should return 60°C is 140 °F', function() {
        expect('60').to.be.equal('140 °F');
    });
})
