'use strict';

const checkTriplets = function(aliceRating, bobRating) {
    let i = 0,
        j = 0,
        aliceMax,
        bobMax;

    for( aliceMax = aliceRating.length; i < aliceMax; i++ ) {
        console.log("aliceRating[i]: ", aliceRating[i]);
    }

    return true;
};

module.exports = {
    checkTriplets
};
