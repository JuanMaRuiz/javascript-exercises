'use strict';

module.exports.checkStr = function (str) {
    const strToArr = str.split(''),
          i = 0;
          max = strToArr.length;

    for ( ; i < max; i +=1 ) {

    }

    return strToArr;
}
